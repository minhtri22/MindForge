# ACO Formal Convergence Review Trigger

Status: **REVIEW EXECUTED / CLOSED**

Triggering evidence:

```text
ACO-1
STOP
TARGET_STABILITY_SUPPORT_INSUFFICIENT
```

Reason:

The finite ACO roadmap preregistered insufficient fresh support as a program STOP condition.

## Review inputs

A future convergence review may use:

- ACO origin audit and charter;
- KCL convergence closure inherited by ACO;
- ACO-1 protocol and immutable lock lineage;
- technical-attempt-1 failure record;
- recovery preflight and lock-v2 verification;
- canonical `FORMAL_RESULT.json`;
- the fact that Y_PRR support was 12 boundaries across stages 1/2/3.

## Review prohibitions

Until that review is explicitly opened:

- do not add seeds to ACO-1;
- do not compute a replacement ACO-1 primary result with relaxed support;
- do not retune thresholds/materiality floors;
- do not open ACO-2;
- do not train an outcome predictor on ACO-1 data;
- do not build a controller;
- do not consume protected KCL confirmatory seeds.

## Review purpose

The review must decide whether:

1. ACO should terminate permanently as formulated;
2. the continuous-outcome question remains independently justified but belongs to a **new research program with a new design and fresh evidence contract**;
3. evidence should instead return to a different upstream continual-learning question.

This document authorizes review only, not new scientific execution.


## Closure

The triggered review has been completed in:

```text
ACO_FORMAL_CONVERGENCE_REVIEW.md
```

Decision:

```text
ACO terminated.
Option 2 selected for future governance:
continuous-response question may reopen only as a separately named new
research program (working name CPRM).
```

No new scientific execution was performed during the review.

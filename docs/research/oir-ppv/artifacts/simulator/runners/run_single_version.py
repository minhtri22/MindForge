"""OIR-PPV simulator runner scaffold.

Purpose:
- load one frozen simulator version
- keep version selection explicit
- avoid hidden architecture changes
"""


def run(version, patterns):
    raise NotImplementedError(
        "Bind runner to frozen simulator module before execution phase."
    )

# M6 Parent Artifact Materialization — Lineage

## 2026-09-22 — Program opened

Origin:

```text
M6 Windows venue evidence:
a78951e4a392aed099c255ad82b3ed6a226de0b6

venue:
PASS at asset/version/server layer

parent artifact:
INVALID_PROVENANCE_PARENT_ARTIFACT_UNAVAILABLE
```

The program is bounded to recovering or materializing one exact copy of the
already-qualified Q4_K_M artifact.

Q4 scientific state is frozen:

```text
Q4_K_M = PASS / CLOSED
Q4 rerun = NOT AUTHORIZED
```

Recovery of pre-existing bytes is preferred. If recovery is exhausted,
deterministic reconstruction is permitted only as artifact materialization under
the exact frozen Q4 identities, with no fixtures, no llama-cli preservation
evaluation and no scientific adjudication.

Admission requires exact frozen byte identity.

M6 create/chat remains closed until parent-artifact admission PASS.


## 2026-09-22 — Recovery-first tooling zero-science gate PASS

Authoritative implementation identity:

```text
a6f9d4341894cf59cab853d4ae6912e060c10599
```

Authoritative zero-science run:

```text
run: 35674627035
job: 106578392811
tests: 12/12 PASS
artifact: 10672272440
artifact zip sha256:
7dd57c54def12afb77c8fb8dd2ee54f196a8e6b3d6d46d85820993cb0b0777c8
```

The recovery-first tool now freezes exact Q4 and F16 artifact identities,
constructs only the exact `Q4_K_M` reconstruction command, and requires a
separate execution-authorization artifact before any copy or quantizer execution.

The planning-only workflow established:

```text
recovery = NOT_FOUND_RECOVERY_EXHAUSTED
reconstruction authorized now = false

existing bytes persisted = false
quantizer executed = false
fixture evaluation executed = false
model inference executed = false
scientific adjudication executed = false
ollama create/chat executed = false
```

Q4 remains `PASS / CLOSED`.

Materialization/reconstruction execution is not authorized by this PASS.

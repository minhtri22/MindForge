# MK-1 Scientific Materialization and Pre-Training Data Audit Result v0.1

Status: **PASS / CANONICAL DATA SPENT AND FROZEN**

Date: **2026-09-21**

Formal verdict:

`SCIENTIFIC_MATERIALIZATION_AND_DATA_AUDIT_PASS`

## 1. Canonical execution identity

Workflow:

`.github/workflows/mk1-materialization-audit.yml`

Canonical one-shot run:

`35553551910`

Execution head:

`13f60afec53252b0ddd0ca7d333f7af2afb1be41`

Authorization:

- `IMPLEMENTATION_LOCK_MANIFEST_V2_PASS`
- `ZERO_FRESH_PREFLIGHT_V2_PASS`
- Amendment 003 frozen before data
- Observable Field Registry frozen before data

No second materialization is authorized.

## 2. Canonical artifact

Artifact:

- name: `mk1-scientific-materialization-v0-1`
- artifact id: `10619711251`
- artifact ZIP SHA-256: `b32084d85c3fd259ef66cdfbd9aed8fb7a48bd2efdea7affbbd6ac1d864dd997`
- ZIP size: `457343` bytes

Artifact contains exactly five files:

- `mk1_materialized_v0_1/train.jsonl`
- `mk1_materialized_v0_1/validation.jsonl`
- `mk1_materialized_v0_1/pristine_confirmatory.jsonl`
- `mk1_pretraining_data_audit.json`
- `mk1_materialization_manifest.json`

## 3. Frozen split identities

### TRAIN

- canonical scenes: `2,000`
- surface records: `4,000`
- bytes: `10,166,148`
- SHA-256: `5bf1b1b5a193ce46baee9aee97e7e03a1e8c2f66bee8216c30cf518a6fb1468a`
- renderer families: A + B
- scene namespace: `7101000..7102999`

### VALIDATION

- canonical scenes: `400`
- surface records: `800`
- bytes: `2,036,424`
- SHA-256: `8c1eed22186d43b3311c7bbb63edfce7ba13880a11c528529bc411b9062a1495`
- renderer families: A + B
- scene namespace: `7103000..7103399`

### PRISTINE_CONFIRMATORY

- canonical scenes: `600`
- surface records: `1,200`
- bytes: `3,071,768`
- SHA-256: `5902fec0e68c296d7fa7463f37f7f0acb4b287718030516d3b67d71f96fc78f6`
- renderer families: A + C
- scene namespace: `7104000..7104599`

Totals:

- canonical scenes: `3,000`
- surface records: `6,000`

## 4. Split / duplicate / renderer integrity

Observed:

- raw-text duplicate count: `0`
- canonical-Z duplicate count: `0`
- cross-split scene-ID overlap: `0`
- wrong split labels: `0`
- renderer-family contract: PASS
- exactly two surfaces per canonical scene: PASS

Result:

**PASS**

## 5. Target stability

Observed:

- surface target invariance failures: `0`
- scalar contract failures: `0`
- frozen C recomposition failures: `0`
- threshold-margin gate:
  `NOT_APPLICABLE_NO_THRESHOLD_DERIVED_PRIMARY_HARD_TARGETS`

All primary hard labels are explicit semantic/categorical properties, not labels generated by thresholding a continuous observed scalar.

Result:

**PASS**

## 6. Observable identifiability

Independent audit parsed every materialized `input_text` without reading generator-only hidden metadata.

Observed:

- observable Z re-derivation failures: `0`
- independent C recomposition failures: `0`
- ambiguous primary scenes: `0`
- forbidden-information reads: `0`

Formal audit state:

`PASS_INPUT_TEXT_ONLY_INDEPENDENT_REDERIVATION`

Result:

**PASS**

This establishes target definability from the current serialized input. It does not establish learnability.

## 7. Support gates

Frozen minima:

- TRAIN >= `100`
- PRISTINE_CONFIRMATORY >= `40`

Observed TRAIN minima:

- minimum primary flat-class support: `180`
- minimum Z4 positive/negative binary cell: `500`
- minimum C1 positive/negative binary cell: `400`
- minimum C5 positive/negative binary cell: `500`

Observed PRISTINE_CONFIRMATORY minima:

- minimum primary flat-class support: `54`
- minimum Z4 positive/negative binary cell: `150`
- minimum C1 positive/negative binary cell: `120`
- minimum C5 positive/negative binary cell: `150`

Confirmatory multi-factor fraction:

`1.0`

H1c exact scope-relation correspondence coverage:

`0.719`

Result:

**PASS**

## 8. Input-size pre-tokenizer observation

Maximum observed raw UTF-8 surface size:

`1,145 bytes`

This is descriptive only.

The frozen model gate is encoded token length <=512, so the exact length gate remains:

`DEFERRED_TO_TRAIN_ONLY_TOKENIZER_FREEZE`

No byte-to-token assumption is used.

## 9. No-training / no-tokenizer evidence

Materialization manifest records:

- `scientific_tokenizer_fitted = false`
- `scientific_training_executed = false`

No scientific model-training seed was used.

No validation or pristine-confirmatory model prediction exists.

## 10. Scientific interpretation

Supported:

- the frozen Amendment-003 generator produced one valid, non-overlapping scientific dataset;
- target stability gates pass on the actual materialized data;
- observable identifiability passes on the actual serialized input;
- primary target support gates pass;
- the three scientific splits are now spent/frozen and cannot be regenerated silently.

Not supported:

- tokenizer compatibility or <=512-token length;
- Z learnability;
- H1a/H1b/H1c;
- any neural training claim.

## 11. Authorization after PASS

The only next scientific action authorized is:

`TRAIN_ONLY_TOKENIZER_FREEZE_AND_TOKENIZED_INPUT_AUDIT`

It must operate on the exact frozen TRAIN/VALIDATION/PRISTINE files identified by the SHA-256 values above.

Tokenizer fitting must use TRAIN surfaces only.

VALIDATION and PRISTINE_CONFIRMATORY may be encoded only after the tokenizer artifact is frozen.

Neural training remains blocked.

name: MSA — MSA-1 execution-lock verification

on:
  workflow_dispatch:
  push:
    branches:
      - research/measurement-substrate-adequacy
    paths:
      - tools/msa/verify_msa1_execution_lock.py
      - tests/test_msa1_execution_lock.py
      - .github/workflows/msa1-execution-lock-verify.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12.14"

      - name: Install exact locked runtime
        run: |
          python -m pip install pip==26.2.1
          python -m pip install numpy==2.3.3 pytest==8.4.2
          python -m pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu

      - name: Independent verifier tests
        run: PYTHONPATH=. python -m pytest tests/test_msa1_execution_lock.py -q

      - name: Run independent static verification
        run: |
          mkdir -p artifacts
          PYTHONPATH=. python tools/msa/verify_msa1_execution_lock.py

      - name: Enforce exact lock verification
        run: |
          python - <<'PY'
          import json
          d=json.load(open("artifacts/msa1_execution_lock_verification.json",encoding="utf-8"))
          assert d["status"]=="PASS", d
          assert d["verdict"]=="MSA1_EXECUTION_LOCK_VERIFICATION_PASS", d
          assert d["lock_sha256"]=="c42062b965a08f5e503f8307eaa27ffbede13511ed245dfdb80e0163d747f657", d
          assert not any(d["collisions"].values()), d
          assert d["checks"]["fresh_execution_workflow_absent"], d
          assert d["checks"]["fresh_collection_absent"], d
          assert d["checks"]["formal_result_absent"], d
          assert d["fresh_seed_execution_attempted"] is False, d
          assert d["scientific_outcome_generated"] is False, d
          assert d["difficulty_mutation_performed"] is False, d
          assert d["predictor_fitting_performed"] is False, d
          print("MSA1_EXECUTION_LOCK_VERIFICATION=PASS")
          PY
          sha256sum artifacts/msa1_execution_lock_verification.json
          sha256sum docs/research/measurement-substrate-adequacy/MSA1_EXECUTION_LOCK.json

      - name: Upload verification evidence
        uses: actions/upload-artifact@v4
        with:
          name: msa1-execution-lock-verification
          path: artifacts/msa1_execution_lock_verification.json
          if-no-files-found: error

-r requirements-pipeline-m2.txt
# M5 uses the already-qualified HF/PyTorch stack for canonical baseline inference.
# llama.cpp converter dependencies live in an isolated venv from the pinned source tree.
jinja2==3.1.6
